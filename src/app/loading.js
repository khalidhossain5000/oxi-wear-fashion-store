import GlobalLoading from '@/components/shared/Loader/GlobalLoading';
import React from 'react';

const loading = () => {
    return (
        <div>
            <GlobalLoading/>
        </div>
    );
};

export default loading;